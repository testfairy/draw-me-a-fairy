apply plugin: 'com.android.application'

android {
    compileSdkVersion 28
    buildToolsVersion '28.0.3'

    defaultConfig {
        applicationId "com.testfairy.samples.drawmefairy"
        minSdkVersion 14
        targetSdkVersion 28
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }

    flavorDimensions "version"
    productFlavors {
        regular {
            dimension "version"
            applicationId "com.testfairy.samples.drawmefairy"
            versionNameSuffix ""

            buildConfigField "String", "APP_TOKEN", "<PUT YOUR APP TOKEN HERE>"
        }

        audio {
            dimension "version"
            applicationId "com.drawmeafairy.audio"
            versionNameSuffix "-with-audio"

            buildConfigField "String", "APP_TOKEN", "<PUT YOUR APP TOKEN HERE>"
        }
    }
}

dependencies {
    implementation 'testfairy:testfairy-android-sdk:1.+@aar'
    //compile 'testfairy:testfairy-android-sdk:release@aar'
    implementation 'com.squareup.okhttp3:okhttp:3.10.0'
}
